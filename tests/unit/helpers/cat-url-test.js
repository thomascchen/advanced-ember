import { catUrl } from 'advanced-ember/helpers/cat-url';
import { module, test } from 'qunit';

module('Unit | Helper | cat url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = catUrl([42]);
  assert.ok(result);
});
